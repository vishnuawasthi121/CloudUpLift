package com.poc.cacheclient.app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class IgniteClientConfig {

	/*
	 * @Bean IgniteClientConfigurer configurer() { // Setting some property. //
	 * Other will come from `application.yml` return cfg -> cfg.setSendBufferSize(64
	 * * 1024); }
	 */
	
	
}
